#include "holberton.h"
/**
 * _strlen - change pointers.
 *@s: value to change
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int count;
	int leng;

	for (count = 0; count < s[count]; count++)
		leng++;
	return (leng);

}
