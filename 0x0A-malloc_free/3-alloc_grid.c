#include "holberton.h"

/**
 * alloc_grid - square
 * @width: line x
 * @height: line y
 * Return: always 0.
 */
int **alloc_grid(int width, int height)
{
	int a = 0, b = 0;

	for ( ; b <= height, b++)
	{
		for ( ; a <= width; a++)
		{

		}
	}
	return (0);
}
