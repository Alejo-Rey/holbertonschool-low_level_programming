#include "holberton.h"
/**
 * reverse_array -  change pointers.
 * @a: value of int
 * @n: number of characters
 * Return: Always 0.
 */
void reverse_array(int *a, int n)
{
	int count, rev;

	for (count = 0; count < n && a[count] != '\0'; count++)
	{
	}
	for (; count  >= 0; count--)
		a[count];
}
